My attempt at a program that prints its own source code. If you want to see how it's made you can have a look through the git history. You can find a simplified version that shows what is happening in explanation.py